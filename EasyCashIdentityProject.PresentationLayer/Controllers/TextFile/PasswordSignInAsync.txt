var result = await _signInManager.PasswordSignInAsync(loginViewModel.UserName, loginViewModel.Password,false,true); 
-burada 3.parametre [bool isPersistens] olarak kullandığımız false=Şifre tarayıcı da hatırlanmasın diyorum.Yani bir nevi beni hatırla chechbox düşünülebilir.
-burada 4.parametre [bool lockoutOnFailure] olarak kullandığımız true=Kullanıcı 5 defa hatalı giriş yapma işleminde bulunduğunda sistemden belirli bir süre giriş yapamıyacaktır.